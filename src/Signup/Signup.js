import React from "react";
import { Button, ThemeProvider } from "react-native-elements";

const Signup = () => {
  return (
    <ThemeProvider>
      <Button title="Signup!" />
    </ThemeProvider>
  );
};

export default Signup;
